# HopeDrops Blood Bank Management System
# Environment Configuration Template
# Copy this file to .env and configure for your environment

# Database Configuration
DB_HOST=localhost
DB_USERNAME=root
DB_PASSWORD=
DB_NAME=bloodbank_db

# Application Configuration
BASE_URL=http://localhost/HopeDrops/
APP_ENV=development

# Security Settings (Generate secure random values for production)
SESSION_SECRET=your-session-secret-here
JWT_SECRET=your-jwt-secret-here

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@hopedrops.com
SMTP_FROM_NAME=HopeDrops

# API Configuration
API_RATE_LIMIT=100
API_TIMEOUT=30

# File Upload Configuration
MAX_UPLOAD_SIZE=5242880
ALLOWED_EXTENSIONS=jpg,jpeg,png,pdf

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Production Deployment Notes:
# 1. Set strong passwords for DB_PASSWORD
# 2. Generate secure random strings for SESSION_SECRET and JWT_SECRET
# 3. Change APP_ENV to 'production'
# 4. Configure proper SMTP settings for email notifications
# 5. Set appropriate file upload limits based on server capacity
# 6. Never commit the actual .env file to version control
